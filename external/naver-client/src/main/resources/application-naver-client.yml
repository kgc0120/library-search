spring:
  config:
    activate:
      on-profile: local

external:
  naver:
    url: https://openapi.naver.com
    headers:
      client-id: ${NAVER_CLIENT_ID}
      client-secret: ${NAVER_CLIENT_SECRET}

